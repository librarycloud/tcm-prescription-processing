export const PACKAGE_STATUS = Object.freeze({
  READY_PICKUP: 0,
  PICKED: 1,
});

export const PICKUP_METHOD = Object.freeze({
  SELF: 0,
  FAMILY: 1,
  DELIVERY: 2,
});

export const PICKUP_METHOD_VALUES = Object.freeze(Object.values(PICKUP_METHOD));
